*.smc
*.sfc
*.srm
*.bps
/build/
# no trailing slash: in agent worktrees these are SYMLINKS to the main
# tree (tools/worktree-setup.sh), and dir-only patterns do not match those
/tools/Mesen.app
/tools/bin
/tools/calypsi/
__pycache__/
/build.ninja
# session/agent state (worktrees can hold ROM copies; never commit)
/.claude/*
!/.claude/commands/
