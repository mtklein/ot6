# supply_waivers.txt -- sanctioned zero-revive cliffs for tools/audit_supplies.py.
#
# One fixture name per line: its drop to zero Fenix Downs across a boundary is
# deliberate and safe.  This list must only SHRINK -- a name that no longer
# names a cliff FAILS the audit as stale, so a fixture that stops dropping
# cannot keep its waiver.  Waive only where the segment ahead genuinely cannot
# lose a member; a supply that ran out on a live route is a finding, not a
# story.
#
# format: <fixture name>   # why the drop is deliberate
